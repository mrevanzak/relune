// This screen is never actually rendered because preventsDefault is true
// It exists only to satisfy expo-router's file-based routing requirement
export default function RecordPlaceholder() {
  return null;
}
